import cv2
import matplotlib.pyplot as plt import torch
from ultralytics import YOLO from google.colab import files
model = YOLO("yolov8n.pt") uploaded = files.upload()
image_path = list(uploaded.keys())[0]

results = model(image_path)

plt.imshow(results[0].plot()) plt.axis('off')
plt.show()
